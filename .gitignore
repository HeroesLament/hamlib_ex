# Elixir / Mix
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/

# Hex package build artifact
*.tar

# Rust / NIF build artifacts
/native/hamlib_nif/target/
**/*.rs.bk

# Compiled NIF (built from source by Rustler on the consumer side)
/priv/native/

# Third-party reference material studied locally (JS8Call-improved sources):
# kept out of the repo — see docs/ANDROID_BRIDGE.md for the distilled notes.
.ref-*

# OS / editor
.DS_Store
*.swp
